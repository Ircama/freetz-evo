# FLAC

  - Package: [master/make/libs/flac/](https://github.com/Ircama/freetz-evo/tree/master/make/libs/flac/)
  - Provides: `libFLAC.so`
  - Used by: `cmus`

The `flac` library package provides the Free Lossless Audio Codec runtime used by `cmus` for lossless local playback.