# libnfs 5.0.3
  - Package: [master/make/libs/libnfs/](https://github.com/Freetz-NG/freetz-ng/tree/master/make/libs/libnfs/)
  - Homepage: [https://github.com/sahlberg/libnfs](https://github.com/sahlberg/libnfs)
  - Provides: `libnfs.so` — NFS (Network File System) client library
  - Used by: —
  - Externalization: supported

libnfs is a client-side NFS library that implements the NFSv2, NFSv3, and NFSv4 protocols. It allows applications to access files on NFS shares without needing the kernel-based NFS client.