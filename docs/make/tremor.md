# tremor (master/HEAD)
  - Package: [master/make/libs/tremor/](https://github.com/Freetz-NG/freetz-ng/tree/master/make/libs/tremor/)
  - Homepage: [https://gitlab.xiph.org/xiph/tremor](https://gitlab.xiph.org/xiph/tremor)
  - Provides: `libvorbisidec.so` — Fixed-point Vorbis decoder
  - Used by: —
  - Externalization: supported

Tremor is a fixed-point implementation of the Vorbis audio decoder, designed for embedded devices without floating-point hardware. It provides the same `libvorbisidec` API as libvorbis.)