# Remove ftpd
Removes the AVM FTP server (can be replaced by the Freetz packages vsftpd or bftpd).<br>
<br>

With this patch, AVM's FTP daemon is removed. As alternatives, bftpd or vsftpd are suitable.

 * The patch can only be selected if the "Show Advanced Options" option was selected beforehand.
