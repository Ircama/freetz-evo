--- linux-4.9/drivers/thermal/thermal_core.c
+++ linux-4.9/drivers/thermal/thermal_core.c
@@ -590,7 +590,8 @@
 	ret = thermal_zone_get_temp(tz, &temp);
 	if (ret) {
 		if (ret != -EAGAIN)
-			dev_warn(&tz->device,
+			/* Freetz: silence read failures of unreadable thermal zone */
+			dev_dbg(&tz->device,
 				 "failed to read out thermal zone (%d)\n",
 				 ret);
 		return;
