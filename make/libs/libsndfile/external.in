config EXTERNAL_FREETZ_LIB_libsndfile
	depends on EXTERNAL_ENABLED && FREETZ_LIB_libsndfile
	bool "libsndfile"
	default n
	help
		externals the following file(s):
		 ${FREETZ_LIBRARY_DIR}/libsndfile.so.1.2.2
