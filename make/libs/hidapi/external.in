config EXTERNAL_FREETZ_LIB_hidapi
	depends on EXTERNAL_ENABLED && FREETZ_LIB_hidapi
	bool "hidapi (libusb)"
	default n
	help
		externals the following file(s):
		 ${FREETZ_LIBRARY_DIR}/libhidapi-libusb.so.0.15.0
