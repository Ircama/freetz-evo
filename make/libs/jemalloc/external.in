config EXTERNAL_FREETZ_LIB_libjemalloc
	depends on EXTERNAL_ENABLED && FREETZ_LIB_libjemalloc
	bool "libjemalloc"
	default n
	help
		externals the following file(s):
		 ${FREETZ_LIBRARY_DIR}/libjemalloc.so.2

