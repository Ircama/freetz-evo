config FREETZ_PACKAGE_RIPGREP
	bool "ripgrep 15.1.0 - DEVELOPER"
	depends on FREETZ_SHOW_DEVELOPER
	default n
	help
		ripgrep (rg) recursively searches files for patterns.

		This package builds ripgrep from source using Rust/Cargo.
