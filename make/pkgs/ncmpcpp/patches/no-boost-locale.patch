--- src/charset.cpp	2024-10-24 14:28:08.000000000 +0200
+++ src/charset.cpp	2026-07-19 10:19:50.358164107 +0200
@@ -18,7 +18,9 @@
  *   51 Franklin St, Fifth Floor, Boston, MA 02110-1301, USA.              *
  ***************************************************************************/
 
-#include <boost/locale.hpp>
+// Note: boost::locale is not available on this embedded target (uClibc).
+// This target runs UTF-8 throughout, so all charset conversions pass through.
+#include <locale>
 #include "charset.h"
 #include "settings.h"
 
@@ -26,59 +28,39 @@
 
 std::locale internalLocale()
 {
-	boost::locale::generator gen;
-	std::locale loc = gen("");
-	bool is_utf = std::use_facet<boost::locale::info>(loc).utf8();
-	std::string name = std::use_facet<boost::locale::info>(loc).name();
-	if (!is_utf && name != "C" && name != "POSIX")
-	{
-		// if current locale does not use unicode, use variant of this
-		// locale with utf8 as ncmpcpp uses utf8 internally and we need
-		// current locale for sorting, case conversions etc.
-		std::string new_name = std::use_facet<boost::locale::info>(loc).language()
-		                     + "_"
-		                     + std::use_facet<boost::locale::info>(loc).country()
-		                     + ".UTF-8";
-		loc = gen(new_name);
-	}
-	return loc;
+	// On uClibc embedded targets we assume UTF-8 throughout.
+	try { return std::locale(""); }
+	catch (...) { return std::locale::classic(); }
 }
 
-std::string toUtf8From(const std::string &s, const char *charset)
+std::string toUtf8From(const std::string &s, const char * /*charset*/)
 {
-	return boost::locale::conv::to_utf<char>(s, charset);
+	// Assume input is already UTF-8 on embedded targets.
+	return s;
 }
 
-std::string fromUtf8To(const std::string &s, const char *charset)
+std::string fromUtf8To(const std::string &s, const char * /*charset*/)
 {
-	return boost::locale::conv::to_utf<char>(s, charset);
+	return s;
 }
 
 std::string utf8ToLocale(const std::string &s)
 {
-	return Config.system_encoding.empty()
-	     ? s
-	     : boost::locale::conv::from_utf<char>(s, Config.system_encoding);
+	return s;
 }
 
 std::string localeToUtf8(const std::string &s)
 {
-	return Config.system_encoding.empty()
-	     ? s
-	     : boost::locale::conv::to_utf<char>(s, Config.system_encoding);
+	return s;
 }
 
 std::string utf8ToLocale(std::string &&s)
 {
-	if (!Config.system_encoding.empty())
-		s = boost::locale::conv::from_utf<char>(s, Config.system_encoding);
 	return std::move(s);
 }
 
 std::string localeToUtf8(std::string &&s)
 {
-	if (!Config.system_encoding.empty())
-		s = boost::locale::conv::to_utf<char>(s, Config.system_encoding);
 	return std::move(s);
 }
 
--- src/ncmpcpp.cpp	2024-10-24 14:28:08.000000000 +0200
+++ src/ncmpcpp.cpp	2026-07-19 10:19:50.358164107 +0200
@@ -24,7 +24,6 @@
 #include <cstring>
 
 #include <boost/date_time/posix_time/posix_time.hpp>
-#include <boost/locale.hpp>
 #include <iostream>
 #include <fstream>
 #include <stdexcept>
@@ -92,7 +91,7 @@
 	using Global::Timer;
 	
 	std::setlocale(LC_ALL, "");
-	std::locale::global(Charset::internalLocale());
+	std::locale::global(Charset::internalLocale()); // uClibc: uses std::locale() internally
 
 	// clog might be overriden in configure, so preserve the original buffer.
 	clog_buffer = std::clog.rdbuf();
--- src/lastfm_service.cpp	2024-10-24 14:28:08.000000000 +0200
+++ src/lastfm_service.cpp	2026-07-19 10:19:50.368164084 +0200
@@ -22,7 +22,6 @@
 
 #include <boost/algorithm/string/replace.hpp>
 #include <boost/algorithm/string/trim.hpp>
-#include <boost/locale/conversion.hpp>
 #include <fstream>
 #include "charset.h"
 #include "curl_handle.h"
--- src/actions.cpp	2024-10-24 14:28:08.000000000 +0200
+++ src/actions.cpp	2026-07-19 22:21:14.288798 +0200
@@ -23,7 +23,6 @@
 #include <cstring>
 #include <boost/date_time/posix_time/posix_time.hpp>
 #include <boost/filesystem/operations.hpp>
-#include <boost/locale/conversion.hpp>
 #include <boost/lexical_cast.hpp>
 #include <algorithm>
 #include <iostream>
@@ -2235,7 +2234,7 @@
 	if (rnd_type != 's')
 	{
 		tag_type = charToTagType(rnd_type);
-		tag_type_str = boost::locale::to_lower(tagTypeToString(tag_type));
+		tag_type_str = tagTypeToString(tag_type);
 	}
 	else
 		tag_type_str = "song";
@@ -2327,7 +2326,6 @@
 		std::string item_type = tagTypeToString(Config.media_lib_primary_tag);
 		myLibrary->Tags.setTitle(Config.titles_visibility ? item_type + "s" : "");
 		myLibrary->Tags.reset();
-		item_type = boost::locale::to_lower(item_type);
 		std::string and_mtime = Config.media_library_sort_by_mtime ?
 		                        " and mtime" :
 		                        "";

--- src/screens/media_library.cpp	2024-10-24 14:28:08.000000000 +0200
+++ src/screens/media_library.cpp	2026-07-19 22:21:14.288798 +0200
@@ -19,7 +19,6 @@
  ***************************************************************************/
 
 #include <boost/date_time/posix_time/posix_time.hpp>
-#include <boost/locale/conversion.hpp>
 #include <algorithm>
 #include <array>
 #include <cassert>
@@ -732,8 +731,7 @@
 				std::make_move_iterator(MPD::SongIterator()));
 			std::sort(list.begin(), list.end(), SortSongs());
 			result = addSongsToPlaylist(list.begin(), list.end(), play, -1);
-			std::string tag_type = boost::locale::to_lower(
-				tagTypeToString(Config.media_lib_primary_tag));
+			std::string tag_type = tagTypeToString(Config.media_lib_primary_tag);
 			Statusbar::printf("Songs with %1% \"%2%\" added%3%",
 				tag_type, Tags.current()->value().tag(), withErrors(result));
 		}
@@ -926,8 +924,7 @@
 			nextColumn();
 		if (Config.titles_visibility)
 		{
-			std::string item_type = boost::locale::to_lower(
-				tagTypeToString(Config.media_lib_primary_tag));
+			std::string item_type = tagTypeToString(Config.media_lib_primary_tag);
 			if (isAlbumOnly)
 			{
 				std::string and_mtime = Config.media_library_sort_by_mtime ? " (sorted by mtime)" : "";
@@ -966,8 +963,7 @@
 		Songs.clear();
 		if (Config.titles_visibility)
 		{
-			std::string item_type = boost::locale::to_lower(
-				tagTypeToString(Config.media_lib_primary_tag));
+			std::string item_type = tagTypeToString(Config.media_lib_primary_tag);
 			if (isAlbumOnly)
 			{
 				std::string and_mtime = Config.media_library_sort_by_mtime ? " (sorted by mtime)" : "";
@@ -1004,8 +1000,7 @@
 	std::string primary_tag = s.get(Config.media_lib_primary_tag);
 	if (primary_tag.empty())
 	{
-		std::string item_type = boost::locale::to_lower(
-			tagTypeToString(Config.media_lib_primary_tag));
+		std::string item_type = tagTypeToString(Config.media_lib_primary_tag);
 		Statusbar::printf("Can't use this function because the song has no %s tag", item_type);
 		return;
 	}

--- src/screens/tag_editor.cpp	2024-10-24 14:28:08.000000000 +0200
+++ src/screens/tag_editor.cpp	2026-07-19 22:21:14.288798 +0200
@@ -22,7 +22,6 @@
 
 #ifdef HAVE_TAGLIB_H
 
-#include <boost/locale/conversion.hpp>
 #include <algorithm>
 #include <fstream>
 
@@ -1050,7 +1049,7 @@
 	{
 		unsigned i = 0;
 		for (std::string tag; !(tag = (s.*m->Get)(i)).empty(); ++i)
-			(s.*m->Set)(boost::locale::to_lower(tag), i);
+			(s.*m->Set)(tag, i);
 	}
 }
 

--- src/screens/tiny_tag_editor.cpp	2024-10-24 14:28:08.000000000 +0200
+++ src/screens/tiny_tag_editor.cpp	2026-07-19 22:21:14.288798 +0200
@@ -22,8 +22,6 @@
 
 #ifdef HAVE_TAGLIB_H
 
-#include <boost/locale/conversion.hpp>
-
 // taglib includes
 #include <fileref.h>
 #include <tag.h>
@@ -201,7 +199,7 @@
 		return false;
 	
 	std::string ext = itsEdited.getURI();
-	ext = boost::locale::to_lower(ext.substr(ext.rfind(".")+1));
+	ext = ext.substr(ext.rfind(".")+1);
 	
 	w.clear();
 	w.reset();

