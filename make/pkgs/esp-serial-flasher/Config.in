config FREETZ_PACKAGE_ESP_SERIAL_FLASHER
	bool "esp-serial-flasher"
	default n
	help
		ESP Serial Flasher for Linux hosts.
		Builds one CLI tool:
		 - linux_flasher: upstream generic CLI (<addr> <file> pairs)
