--- a/fatresize.c
+++ b/fatresize.c
@@ -74,7 +74,11 @@ static void usage(int code) {
           "Please report bugs to %s\n",
           PACKAGE_NAME, PACKAGE_BUGREPORT);
 
-  exit(code);
+  /* Avoid libparted teardown path after printing usage; some static-link
+   * combinations crash in global destructors on target.
+   */
+  fflush(NULL);
+  _exit(code);
 }
 
 static void printd(int level, const char *fmt, ...) {
