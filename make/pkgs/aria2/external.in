config EXTERNAL_FREETZ_PACKAGE_ARIA2
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_ARIA2
	bool "aria2"
	default n
	help
		externals the following file(s):
		 /usr/bin/aria2c
		 ${FREETZ_LIBRARY_DIR}/libaria2.so* (if libaria2 enabled)
		 /usr/include/aria2/ (if libaria2 enabled)